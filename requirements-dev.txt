# Local development / CI tooling. Installs production deps plus lint, format,
# audit, and load-test tools. Production images and Render install only
# requirements.txt to keep the runtime lean.
-r requirements.txt
ruff==0.16.5
black==26.5.1
isort==9.0.1
pip-audit==2.10.1
locust==2.46.5
